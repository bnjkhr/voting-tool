# Vercel Environment Configuration
NODE_ENV=production
PORT=3000

# Firebase Configuration (wird durch Vercel Environment Variables ersetzt)
FIREBASE_PROJECT_ID=${FIREBASE_PROJECT_ID}
FIREBASE_CLIENT_EMAIL=${FIREBASE_CLIENT_EMAIL}
FIREBASE_PRIVATE_KEY=${FIREBASE_PRIVATE_KEY}

# Email Configuration (wird durch Vercel Environment Variables ersetzt)
EMAIL_USER=${EMAIL_USER}
EMAIL_PASSWORD=${EMAIL_PASSWORD}
BASE_URL=${BASE_URL}

# Admin Configuration (wird durch Vercel Environment Variables ersetzt)
ADMIN_PASSWORD=${ADMIN_PASSWORD}
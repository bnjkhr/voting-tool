{
  "name": "voting-tool-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed - Node.js API only'",
    "vercel-build": "cd voting-tool && npm install --production"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
